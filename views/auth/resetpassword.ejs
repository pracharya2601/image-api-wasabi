<div class="p-2">
    <form action="/resetpassword" method="POST" class="p-2">
        <% if (message) { %>
            <p class="mt-10 text-blue-500 bg-gray-800 px-2 py-2 rounded"><%= message %></p>
        <% } %>
        <%- include('../partials/authform/index.ejs', {type: 'resetpassword'}) %>
    </form>
    <div class="w-full max-w-md p-4">
        <div class="flex flex-wrap -mx-3 mb-6">
            <div class="w-full px-3 mb-6 md:mb-0">
                Go back to <a href="/login" class="text-blue-500 hover:text-blue-600 hover:underline">Login</a>
            </div>
        </div>
    </div>
    <script>
        
    </script>
</div>
